interface:
  display_name: "Embedded Project Bootstrap"
  short_description: "Bootstrap reproducible embedded C projects"
  default_prompt: "Use $embedded-project-bootstrap to configure a clean project for my chosen MCU, RTOS, SDK, and toolchain."

policy:
  allow_implicit_invocation: false
